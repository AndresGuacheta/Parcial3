version: 2.1
jobs:
 build:
    docker:
      - image: cimg/base:2021.04
    steps:
      - checkout
      - add_ssh_keys:
          fingerprints:
            - "f0:e8:42:28:8b:62:f6:24:1e:64:cf:7c:78:9a:a9:5c"
      - run: sudo apt-get update && sudo apt-get upgrade -y
      - run:
          name: "Comandos para deploy en desarrollo"
          command: |
            sudo apt install tidy -y
            git clone --branch circleci-project-setup https://github.com/AndresGuacheta/Parcial3.git
            cd Parcial3/
            tidy index.html
            scp -o StrictHostKeyChecking=no index.html  Bastion@157.55.181.62:/home/Bastion/
  
   
workflows:
   version: 2
   Parcial3ITI:
     jobs:
       - build
